# Researcher agent configuration
# Specializes in gathering information and augmenting state with findings

options:
  agents:
    researcher:
      name: Researcher
      model:
        name: llama3.2
      capabilities:
        - augment
        - evaluate
      system_instructions:
        $type: Optify.ConfigurableString
        base:
          file: researcher/system_instructions.liquid
