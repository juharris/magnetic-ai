# Writer agent configuration
# Specializes in summarizing findings and finalizing outputs

options:
  agents:
    writer:
      name: Writer
      model:
        name: llama3.2
      capabilities:
        - summarize
        - finalize
      system_instructions:
        $type: Optify.ConfigurableString
        base:
          file: writer/system_instructions.liquid
