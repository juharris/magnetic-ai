See [config/instructions.yaml](../config/instructions.yaml) for the evaluation criteria.

Use actions or tools to browse sites like the GitHub repo.
Use cURL if necessary.

Check files for the criteria such as checking if Gemini was used.

